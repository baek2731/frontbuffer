# Frontbuffer 수정사항 적용 가이드

## 수정된 파일 목록
| 파일 | 수정 내용 |
|------|----------|
| `step2_plan.yml` | Step 3 자동 트리거 추가 (이슈 ① 해결) |
| `step3_write.yml` | SLUG 생성 로직 통일 (이슈 ④ 해결) |
| `publish_one.py` | FIFO 기준 st_mtime → 파일명 알파벳 순 (이슈 ③ 해결) |
| `gemini_api.py` | 품질 미달 시 exit → 경고만 출력 (이슈 ⑤ 해결) |
| `fix_w30_pipeline.py` | W30 중복 클러스터 정리 스크립트 (이슈 ② 해결) |

---

## 1단계: 깃 동기화

```bash
cd C:\Users\B\Projects\blogauto2

# remote가 없는 경우에만 실행 (이미 있으면 건너뜀)
git remote add origin https://github.com/baek2731/frontbuffer.git

# 최신 상태로 맞추기
git fetch origin
git checkout main
git pull origin main --rebase
```

---

## 2단계: 수정 파일 교체

다운로드한 파일을 로컬 프로젝트 폴더에 복사:

```
C:\Users\B\Projects\blogauto2\
  ├── .github\workflows\
  │     ├── step2_plan.yml     ← 교체
  │     └── step3_write.yml    ← 교체
  ├── publish_one.py           ← 교체
  ├── gemini_api.py            ← 교체
  └── fix_w30_pipeline.py      ← 신규 추가
```

---

## 3단계: W30 중복 클러스터 정리

```bash
cd C:\Users\B\Projects\blogauto2
python fix_w30_pipeline.py content_pipeline.json
```

출력 예시:
```
  ❌ rejected: Samsung Galaxy Foldables
  ❌ rejected: Samsung Galaxy Foldable Phones & Wearables
  ...
  ✅ 유지:     Samsung Galaxy Z Fold 8 [A]
  ✅ 유지:     Samsung Galaxy Fold Flip Series [A]
  ✅ 유지:     Google Android Ecosystem [A]

총 13개 클러스터 rejected 처리 완료.
남은 candidate: 3개
```

---

## 4단계: 커밋 & 푸시

```bash
cd C:\Users\B\Projects\blogauto2

git add .github/workflows/step2_plan.yml
git add .github/workflows/step3_write.yml
git add publish_one.py
git add gemini_api.py
git add fix_w30_pipeline.py
git add content_pipeline.json

git commit -m "fix: Step2→3 자동연쇄 / W30 중복클러스터 / FIFO / SLUG / gemini품질처리"
git push origin main
```

---

## 5단계: W30 Step 3 수동 실행

GitHub → Actions → Step 3 — Write Articles → Run workflow

완료 후 Discord에서 확인:
- 글 3편 생성 완료 알림
- 품질 체크 결과

---

## 수정 내용 상세

### ① Step 2→3 자동 연쇄 (step2_plan.yml)
`workflow_run`은 수동 실행을 감지 못하는 GitHub 제약.
→ Step 2 성공 시 `actions/github-script`로 Step 3를 직접 dispatch.

### ② W30 중복 클러스터 (fix_w30_pipeline.py)
같은 hub_keyword를 가진 클러스터 13개 → rejected.
남은 candidate 3개: Galaxy Z Fold 8 / Fold Flip Series / Android Ecosystem.

### ③ FIFO 기준 (publish_one.py)
GitHub Actions에서 git checkout 시 st_mtime이 초기화됨.
→ 파일명 알파벳 순 정렬로 변경 (일관된 순서 보장).

### ④ SLUG 생성 통일 (step3_write.yml)
bash의 `tr/sed` vs Python `slugify()`가 달라서 NEW_FILES 추적 실패 → 전체 재체크.
→ python slugify()로 통일.

### ⑤ gemini_api.py 품질 처리
600단어 미달 시 exit(1) → Step 3 루프 전체 실패.
→ 경고만 출력하고 저장 진행 (review 단계에서 보강 가능).
